const hikeExp = document.querySelector("hike-exp");

let observer = new IntersectionObserver(fn, options)

let options = {
    threshold: 0.5
}

let observer = interpersonal(fn, options)

observer.observe(slide)

observer.observe(slide)